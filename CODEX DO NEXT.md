Work in the active repo only: C:\Users\donny\Desktop\futures_intraday_model.

Follow AGENTS.md exactly. Before edits, inspect repo path and `git status --short`.
Keep diffs minimal. Do not stage, commit, delete, move, rename, add dependencies,
or rewrite architecture unless explicitly asked. Do not delete generated `data/`
or `reports/` artifacts. Generated artifacts may be regenerated by validation but
must remain ignored/untracked.

Goal:
Build canonical Phase 4 coverage for `tier_1_research` so the next bounded WFA
run can use `data/feature_matrices/baseline` for the required core research
market-years.

Scope:
- Canonical Phase 4 feature root:
  `data/feature_matrices/baseline/{market}/{year}.parquet`
- `tier_1_research` markets/years come from `configs/alpha_tiered.yaml`.
- Do not run full tier_3 Phase 7.
- Do not tune models or hyperparameters.
- Do not present sample or partial artifacts as production/live evidence.

Tasks, in order:

1. Audit current canonical coverage
   - Inspect `configs/alpha_tiered.yaml` for the resolved `tier_1_research`
     markets and years.
   - Audit existing Phase 3 labeled inputs under `data/labeled/{market}/{year}.parquet`.
   - Audit existing Phase 4 outputs under
     `data/feature_matrices/baseline/{market}/{year}.parquet`.
   - Report present/missing labeled inputs and present/missing canonical Phase 4
     outputs.
   - If required labeled inputs are missing, stop and report the exact missing
     files instead of fabricating or broadening scope.

2. Build missing canonical Phase 4 outputs only
   - Run Phase 4 only for the smallest profile/scope that produces the missing
     `tier_1_research` canonical outputs.
   - Use:
     `--input-root data/labeled`
     `--output-root data/feature_matrices/baseline`
     `--reports-root reports/features_baseline`
   - Do not use stale non-canonical `data/feature_matrices/{market}/{year}.parquet`
     as success evidence.
   - Do not delete stale non-canonical artifacts.

3. Validate coverage and manifests
   - Confirm fresh Phase 4 manifest/report paths reference:
     - `input_root = data/labeled`
     - `output_root = data/feature_matrices/baseline`
   - Confirm model target columns physically exist in each built Phase 4 output:
     - `target_fade_success_15m`
     - `target_trend_danger_30m`
   - Confirm target columns are not in
     `data/feature_matrices/baseline/feature_cols.json`.
   - Report row counts, warning_count, failure_count, and generated manifest paths.

4. Run bounded WFA readiness smoke
   - Build a Phase 5 split plan for `tier_1_research` using:
     `--input-root data/feature_matrices/baseline`
   - If Phase 5 succeeds and produces selectable research folds, run Phase 7 with
     `--max-folds 1` only.
   - If convergence/collapse gates fail, report the failure as a valid hard gate;
     do not tune model settings in this task.

Validation:
- Run focused Phase 4 tests if Phase 4 code changes are made.
- Run `python -m py_compile` for any modified scripts.
- Run `git check-ignore -v` on generated `data/` and `reports/` outputs.
- Run `git diff --check`.
- Run `git status --short`.

Final report:
- Files changed.
- Exact commands run.
- Tests/checks run and pass/fail.
- Coverage counts for `tier_1_research`.
- Generated artifact paths and ignored/untracked status.
- Unresolved risks or blockers.
